# Only the new simple prompt-injection baseline. This intentionally reuses the
# existing runs directory without scheduling any previously completed methods.

[execution]
python = "python"
runs_dir = "runs_additional_baseline"
cache_dir = ".biaschain_cache"
max_retries = 2
retry_backoff_seconds = 30
stage_timeout_minutes = 0
fail_fast = false
stance_model = "ollama:gemma4:latest"

[matrix]
datasets = ["wiki_balance"]
generators = ["ollama"]
attack_methods = ["simple_prompt_injection"]
target_stances = ["PRO", "CON"]
embedders = ["nomic", "qwen"]
agentic_rag = [false]
n_questions = 40
runs_per_question = 10
seed = 42
top_k = 5

[dataset.wiki_balance]
query_file = "data/naturalqueries.jsonl"
corpus_files = ["data/naturalcorpus.jsonl"]
collection_name = "naturalcorpus"

[dataset.wiki_balance.clean_databases]
nomic = "chroma_db_nomic_natural"
qwen = "chroma_db_qwen3_embedding_4b_natural"

[dataset.procon]
query_file = "data/procon_queries.jsonl"
corpus_files = ["data/procon_corpus.jsonl"]
collection_name = "procon"

[dataset.procon.clean_databases]
nomic = "chroma_db_nomic_procon"
qwen = "chroma_db_qwen3_embedding_4b_procon"

[model.ollama]
name = "llama3.1:8b"

[embedder.nomic]
name = "nomic-embed-text"

[embedder.qwen]
name = "qwen3-embedding:4b"

[baseline.simple_prompt_injection]
# Leave blank to sample this dataset's topics through intent_agent. Set a path
# here only when intentionally reusing an external topic/result file.
source_file = ""
